import React, { Component } from 'react';
import { StyleSheet, Text, View, Image, NetInfo } from 'react-native';

class MessageForm extends Component {
    render() {
        return (
            <Text>Formulario para enviar observaciones</Text>
        );
    }
}

export default MessageForm;